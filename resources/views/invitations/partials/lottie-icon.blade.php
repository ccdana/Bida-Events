<span
    class="invitation-lottie {{ $class ?? 'w-6 h-6' }}"
    data-lottie-icon="{{ $name }}"
    @if(!empty($stroke)) data-lottie-stroke="{{ $stroke }}" @endif
    role="img"
    aria-hidden="true"
></span>
